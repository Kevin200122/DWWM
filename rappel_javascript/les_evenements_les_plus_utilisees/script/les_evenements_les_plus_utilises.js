//window.addEventListener('load', () => {
    //console.log("Le site à chargé !");//Evenement se faisant au chargement de la page.
//})
//windows.addEventListener('DOMContentLoaded', () => {//Evenement se faisant au commencement du chargement de la page.
    //les images se chargerons en dernier si il y en a dans notre page.
   // console.log("Le DOM est entrain de se charger, avant les images !");
//})
//window.addEventListener('scroll', () => {
    //console.log("Tu viens de scroll ! ")//Quand la souris scroll sur un ou des éléments de la page .
//})
//window.addEventListener('keydown', () => {//Quand nous appuyons sur une touche l'événement se déclanche.
    //console.log("Tu viens d'appuyer sur une touche .")
//})
/*window.addEventListener('keyup', (e) => {//Se déclanche au moment ou nous arretons d'appuyer sur la touche.
    console.log(e);*/
//})
//document.getElementById('inp').addEventListener
//('input', () => {//L'événement se déclanche dès que nous remplissons notre input.
    //console.log("L'input reçoit des données !");
//})

//document.querySelector('form').addEventListener
//('submit', (e) => {
   // e.preventDefault();
    //console.log("Formulaire envoyé !");//Se déclanche en appuyant sur la bouton valider.
//})